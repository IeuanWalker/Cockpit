<div class="border-b border-vscode-border">
    <button class="w-full px-3 py-2 flex items-center justify-between hover:bg-vscode-hover text-left" @onclick='ToggleExpander'>
        <div class="flex items-center">
            @Icon
            <span class="text-xs font-medium">@Title</span>
        </div>
        <svg class="w-3 h-3 transform transition-transform @(IsOpen ? "rotate-180" : string.Empty)" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
        </svg>
    </button>
    <div class="dropdown-content @(IsOpen ? "open" : string.Empty)">
        <div class="dropdown-inner">
            @Content
        </div>
    </div>
</div>
