@namespace Cockpit.Components.Controls

<!-- Colored circle component for selected model - shows exact color -->
<div class="w-3 h-3 rounded-full" title="@GetTooltip()"
     style="background-color: @(Color ?? "#888888"); flex-shrink-0;">
</div>