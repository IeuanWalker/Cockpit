@using XtermBlazor

@if(IsOpen)
{
    <div class="terminal-panel bg-vscode-sidebar" style="height: 30%">
        <!-- Chat Header -->
        <div class="h-10 bg-vscode-sidebar border-b border-vscode-border px-3 flex items-center">
            <div class="flex items-center flex-1">
                <h1 class="text-vscode-title font-semibold text-sm flex items-center">
                    Terminal
                </h1>
            </div>

            <div class="flex items-center ml-auto">
                <!-- Reset -->
                <button @onclick="async () => await Reset()" class="p-1.5 hover:bg-vscode-hover rounded" title="Reset">
                    <svg class="icon-sm" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <rect x="3" y="3" width="18" height="18" rx="2" stroke-width="2" />
                        <path d="M8 9l3 3-3 3" stroke-width="2" />
                        <path d="M13 15h3" stroke-width="2" />
                    </svg>
                </button>
            </div>
        </div>

        <Xterm @ref="_terminal"
               Options="_options"
               Id="@_terminalId"
               Addons="_addons"
               OnFirstRender="OnTerminalFirstRender"
               OnData="OnTerminalInput"
               OnResize="OnTerminalResize" />
    </div>
}