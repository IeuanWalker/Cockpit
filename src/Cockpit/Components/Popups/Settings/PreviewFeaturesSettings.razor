<div class="settings-section">
	<h4 class="text-sm font-semibold text-app-title mb-3">Preview features</h4>

	<div class="diag-card">
		<div class="diag-settings-grid">
			<label class="diag-toggle-row">
				<div>
					<div class="text-sm font-medium text-app-title">Canvas</div>
					<div class="text-xs secondary-text">Enable canvas support — opens interactive windows during agent sessions. This is an experimental feature from the Copilot SDK and behaviour may change.</div>
				</div>
				<span class="diag-toggle-control">
					<input type="checkbox" @bind="_canvasEnabled" @bind:after="OnCanvasChanged" />
					<span class="diag-toggle-track"></span>
				</span>
			</label>
		</div>
	</div>

	@if(_showRestartWarning)
	{
		<div class="settings-restart-warning mt-3">
			<svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
				<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" />
			</svg>
			<span class="text-xs">Restart the app for this change to take full effect.</span>
		</div>
	}
</div>
